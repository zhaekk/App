<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.app">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Set Dashboard activity -->
        <activity android:name=".Dashboard" android:exported="true" />

        <!-- Set Login activity as the main activity -->
        <activity android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Calendar Activity -->
        <activity android:name=".Calendarclass" />

        <!-- Recommendations Activity -->
        <activity android:name=".RecoMainActivity" />

        <!-- Analytics Activity -->
        <activity android:name=".AnalyticMainActivity" />

        <!-- PestMainActivity -->
        <activity android:name=".PestMainActivity" />


        <!-- Harvest Volume Activity -->
        <activity android:name=".HarvestVolMainActivity" />

        <!-- Tutorial Activity -->
        <activity android:name=".TutorialMainActivity" />

        <!-- Chatbot Activity -->
        <activity android:name=".ChatbotMainActivity" />

    </application>

</manifest>
